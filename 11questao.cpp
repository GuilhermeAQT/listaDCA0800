#include <stdio.h>

int main(void){
	float zinco,cobre, latao;
	printf("Digite a quantidade de latao:");
	scanf("%f", &latao);
	printf("A quantidade de zinco e: %2.f\n", zinco = latao*0.3);
	printf("A quantidade de cobre e: %2.f", cobre = latao*0.7);
	
	return 0;
}
